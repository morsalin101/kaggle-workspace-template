"""kwt — Kaggle Workspace Tool.

A thin, opinionated wrapper around the `kaggle` CLI that lets one repo drive
many Kaggle notebooks, each described by a readable `config.yml`.
"""

__version__ = "1.0.0"
