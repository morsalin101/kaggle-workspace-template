"""Code shared by every project in this workspace.

Anything here is bundled alongside a project's own `src/` when that project
sets `src.include_shared: true` in its config.yml. On Kaggle it imports as
`shared.<module>`.
"""
